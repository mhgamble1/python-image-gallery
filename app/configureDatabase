#!/bin/bash

PASSWORD_FILE=$IG_PASSWD_FILE
DUMP_FILE="config_db.sql"
read -r PASSWORD < $PASSWORD_FILE
export PGPASSWORD=$PASSWORD
psql -h $PG_HOST -U $IG_USER -d $IG_DATABASE -f $DUMP_FILE
unset PGPASSWORD
